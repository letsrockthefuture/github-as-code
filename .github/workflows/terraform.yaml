name: Terraform

on:
  workflow_dispatch:
  push:
    branches:
      - test/reusable_workflows
    paths:
      - ".github/workflows/terraform.yaml"
      - "terraform/**"
  pull_request:
    branches:
      - test/reusable_workflows
    paths:
      - ".github/workflows/terraform.yaml"
      - "terraform/**"

jobs:
  terraform:
    uses: letsrockthefuture/reusable-workflows/templates/terraform/workflow.yaml@main

##